external:
  samples_path: data/external_samples.npz
  max_samples: 75000000
  shuffle: true
  validation_split: 0.1
  seed: 42
  dedup: true
  filter_invalid: true
  drop_zero_states: true
  checkpoint_prefix: external
  save_dir: models/external
  benchmark_games: 8

training:
  buffer_size: 5000000
  batch_size: 64
  epochs: 2
  train_steps_per_iter: 4000

system:
  checkpoint_path: models/external/external_best_model.pth
